# ============================================================================
# Polymarket 跟单交易机器人 - 配置文件
# ============================================================================
# 将此文件复制为 .env 并填写您的值
# 永远不要将您的 .env 文件提交到 git（它包含敏感信息）

# ============================================================================
# 必需设置（必须填写这些）
# ============================================================================

# 您的钱包私钥（64 字符十六进制字符串，不带 0x 前缀）
#  请保密！永远不要分享或提交到 git！
# 示例：0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef
PRIVATE_KEY=your_private_key_here

# 您的钱包地址（40 字符十六进制字符串，可以包含或不包含 0x 前缀）
# 这应该与您的 PRIVATE_KEY 对应的钱包匹配
# 示例：0x1234567890123456789012345678901234567890
# 或：     1234567890123456789012345678901234567890
FUNDER_ADDRESS=your_wallet_address_here

# 您要复制交易的鲸鱼地址（40 字符十六进制，不带 0x 前缀）
# 在 Polymarket 上找到成功的交易者并使用他们的钱包地址
# 示例：204f72f35326db932158cba6adff0b9a1da95e14
TARGET_WHALE_ADDRESS=target_whale_address_here

# WebSocket RPC 提供商（选择一个 - 推荐初学者使用 Alchemy）
# 从以下地址获取免费 API 密钥：https://www.alchemy.com/
ALCHEMY_API_KEY=your_alchemy_api_key_here

# 替代方案：使用 Chainstack 代替 Alchemy（将 ALCHEMY_API_KEY 留空）
# 从以下地址获取免费 API 密钥：https://chainstack.com/
# CHAINSTACK_API_KEY=your_chainstack_api_key_here

# ============================================================================
# 交易设置（可选 - 显示默认值）
# ============================================================================

# 启用实际交易（true）或仅监控（false）
# 测试时设置为 false 以防止意外交易
ENABLE_TRADING=true

# 模拟交易模式 - 模拟交易而不实际执行
# 适合测试：设置为 true 以查看机器人会做什么
MOCK_TRADING=false

# ============================================================================
# 断路器设置（高级 - 可选）
# ============================================================================
# 这些设置可保护您免受在危险市场条件下复制交易的风险

# 触发断路器分析的最小交易规模（股数）
# 默认值：1500.0（低于此值的交易跳过断路器）
CB_LARGE_TRADE_SHARES=1500.0

# 触发断路器的连续大额交易数量
# 默认值：2（如果在时间窗口内有 2+ 笔大额交易，检查订单簿深度）
CB_CONSECUTIVE_TRIGGER=2

# 检查连续大额交易的时间窗口（秒）
# 默认值：30（检查最近 30 秒）
CB_SEQUENCE_WINDOW_SECS=30

# 鲸鱼价格之外所需的最小订单簿深度（USD）
# 如果深度较低，断路器将阻止交易
# 默认值：200.0
CB_MIN_DEPTH_USD=200.0

# 断路器触发后阻止交易的时间（秒）
# 默认值：120（2 分钟）
CB_TRIP_DURATION_SECS=120

# ============================================================================
# 注意事项
# ============================================================================
# 
# 1. 所有带有 "here" 的值都需要替换为您的实际值
# 2. 删除行首的 "#" 以启用该设置
# 3. 布尔值：true/false（不区分大小写）或 1/0
# 4. 如果您不确定可选设置的作用，请保持原样
# 5. 有关详细说明，请参阅 docs/CONFIGURATION.md
